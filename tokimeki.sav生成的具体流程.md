### 🔄 正常游戏进程中，存档写入与校验的“生命周期”：

#### 第一步：数据准备（主程序在内存中写明文）
当玩家在游戏里点击“存档”时：
1.  **`tokimeki.exe`（主程序）**在游戏运行的内存中，将你最新的能力值（比如 284 体力）整理好。
2.  此时，主程序内存里的数据全部都是**没有加密的明文**。（这也是为什么FPE或者CheatEngine能快速检索到内存中的属性值）

#### 第二步：召唤卫士（主程序启动外部进程）
1.  主程序自己**不进行任何文件加密和校验和计算**。
2.  它通过 `WinExec` 启动 **`tbta.exe`（外部校验程序）**，并把当前选择的“槽位号”等参数传递给它。

#### 第三步：计算、加密与落盘（全部由 `tbta.exe` 独立完成！）
这就是我们在逆向 `tbta.exe` 时看到的底层逻辑：
1.  **接收并拼装**：`tbta.exe` 接收到主程序传过来的明文数据，将其放入它自己的大内存缓冲区 `&DAT_0044b430`。
2.  **计算校验和（谁来计算并填写？）**：
    *   `tbta.exe` 调用内部的 **`FUN_004096d0` 算法**，将明文数据按字节全部累加，算出一个字节的总和。
    *   它亲自把算好的校验和写入明文对应的校验和位置。
3.  **加密（谁来加密？）**：
    *   `tbta.exe` 调用加密函数 **`FUN_00401f60(1, ...)`**，对整个内存缓冲区进行 **循环右移 1 位（ROR 1）** 加密（这把校验和也顺便给加密了）。
4.  **落盘写入**：
    *   `tbta.exe` 调用 `__write` 将加密后的整块密文数据写入硬盘上的 `tokimeki.sav`。
5.  **发送绿灯信号**：
    *   写入成功后，`tbta.exe` 往临时文件 `twinbee.chk` 里写入一个代表成功的数字 `1`。

#### 第四步：完成反馈
主程序 `tokimeki.exe` 检测到 `twinbee.chk` 变为了 `1`，知道外部安全卫士已经安全、合法地完成了加密和存盘，于是在游戏界面上向玩家弹出“存档成功”的提示。

---

### 💡 总结成一句话：
**在正常游戏中，“计算校验和”、“加密数据”以及“最终将数据写入 sav 硬盘文件”的动作，全部都是由外部安全程序 `tbta.exe` 在后台独立计算并填写进去的。** 

这也就是为什么主程序里完全找不到任何加密和校验代码的终极原因。
