# 《心跳回忆》一代中文版存档（.sav）底层加密与校验机制破译报告

很多朋友尝试用十六进制编辑器（如 HxD）直接修改《心跳回忆》的存档，但往往一改属性，载入游戏时所有存档槽位就会变成**一片空白**。这是因为游戏内部设计了**“数据位移加密”**和**“外部进程校验（tbta.exe）”**双重保护。

以下是本次通过反编译工具彻底破译的数据演变与校验运行机制：

---

### 一、 存档文件的宏观结构（位置在哪里？）

游戏包含 14 个存档槽位，全部挤在同一个 `tokimeki.sav` 文件中。
*   **存档大小**：每个槽位的大小固定为 **`0xA770` 字节**（十进制 42,864 字节）。
*   **槽位位置**：
    *   **Slot 1** 起始地址：`0x00000060`
    *   **Slot 2** 起始地址：`0x0000A7D0`
    *   **Slot $N$** 起始地址：`0x60 + (N - 1) * 0xA770`

每个槽位内部的属性（如体力、文科等）是等距排列的，每个属性占用 **4 个字节**（这在计算机里称为一个“双字”）。

---

### 二、 数据的“遮罩”：循环移位加密（为什么直接改会坏？）

为了防止玩家直接修改，游戏在保存时，将存档中的数据在二进制下**“向右循环挪了 1 位”**（即 `ROR 1` 加密）。

*   **例子**：如果明文数据是 `0110`（二进制），右挪 1 位后在硬盘上就变成了 `0011`。
*   **后果**：如果你直接在 HxD 里把数值改成 `0011`，游戏读取时会执行解密（向左挪 1 位），数据就会变成完全错乱的垃圾值，进而导致游戏崩溃。
*   **正确修改公式**：
    $$\text{HxD 密文} \xrightarrow{\text{左挪 1 位解密}} \text{明文数据} \xrightarrow{\text{修改数值}} \text{新明文} \xrightarrow{\text{右挪 1 位加密}} \text{写入 HxD}$$

---

### 三、 数值在内存中的摆放规则（明文数据长什么样？）

当我们将 4 字节的数据“向左循环挪 1 位”解密后，会得到它真实的明文结构。因为计算机存储数据是倒着放的（小端序），解密后的 4 个字节从左到右的物理意义是：

$$\mathbf{[保留位]\ [小数位]\ [整数低位]\ [整数高位]}$$

*   **1. 正常数值（255 以下）的摆放**：
    例如文科初始值 **40**（十六进制为 `28`），明文就是：`[00] [00] [28] [00]`。
*   **2. 大数值（突破 255）的进位摆放**：
    例如实测的体力值 **284**（十六进制为 `011C`）。因为超过了单字节最大值 255，它必须拆分成低位 `1C` 和高位 `01`，明文为：
    `[00] [小数位] [1C] [01]`（高低位组合起来 `011C` 刚好是十进制的 284）。
*   **3. 修改大数（如 600）**：
    600 转换为十六进制是 `0258`（低位 `58`，高位 `02`）。
    我们要构建的新明文就是：`[00] [00] [58] [02]`。
    重新加密（向右挪 1 位）后，写入 HxD 即可。

---

### 四、 全局校验和与“字节分裂悖论”（为什么改了会显示空白？）

游戏在运行、保存或载入时，会调用一个后台安全程序 **`tbta.exe`**：
1.  `tbta.exe` 启动后，会将整个 `tokimeki.sav` 读入内存并全部解密。
2.  它会将解密后的存档数据进行**“全员大累加”**（将每个字节的值一个接一个加起来，溢出部分不要，只取最后 1 个字节，这叫 **8-bit Checksum**）。
3.  它将算出的总和与文件末尾 `0x92864` 处的“密文校验和”进行比对。如果比对失败，游戏主程序就会判定存档损坏，**在界面上把所有槽位强制显示为空白**。

#### 💡 核心突破：免重算整档的“差值补偿法”
因为校验和只是所有字节的简单加法，我们不需要重新计算整个 600KB 文件的总和。**我们只需计算被修改属性前后的“明文字节和差值 (Delta)”**。

但这产生了一个非常反直觉的 **“字节分裂悖论”**：
> **“为什么我把体力值从 124.5 增加到 233.0，校验和差值反而减少了 19 呢？”**

#### 🔍 悖论背后的数学真相：
因为属性在明文里被分成了**整数位**和**小数位**独立相加：
*   **原体力（124.5）明文**：`[00] [80] [7C] [00]` $\rightarrow$ 字节相加 = $0 + 128（小数）+ 124（整数）+ 0 = 252$。
*   **新体力（233.0）明文**：`[00] [00] [E9] [00]` $\rightarrow$ 字节相加 = $0 + 0（小数抹零）+ 233（整数）+ 0 = 233$。
*   **差值计算**：
    *   **整数部分**：从 124 增加到 233，获得了 **`+109`** 的字节和。
    *   **小数部分**：原本的 `0x80`（0.5）被我们抹零变成了 `0x00`，失去了 **`-128`** 的字节和。
    *   **最终结果**：
        $$\text{实际变化量} = +109 - 128 = \mathbf{-19}$$

由于我们修改属性时，顺手将原存档里的 `.5` 小数（等价于 128 的字节和）给抹零了。因此，**虽然数值在面板上变大了，但整体明文字节总和反而净减少了 19！**

我们只需要去文件末尾 `0x92864`，将原加密校验和解密，**直接减去 19**，然后再重新加密写回。这样，整体文件的 Sum 与末尾的校验和实现了完美守恒，游戏就会直接放行！
